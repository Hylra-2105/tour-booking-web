extends ../layouts/default.pug
include ../mixins/checkbox.pug

block title
  title Tạo nhóm quyền

block main
  h1.box-title Tạo nhóm quyền

  // Section 8
  .section-8
    form(action="")
      .inner-group 
        label.inner-label(for="roleGroup") Tên nhóm quyền
        input(type="text" id="roleGroup" name="roleGroup" value="Quản trị viên")
      .inner-group 
        label.inner-label(for="shortDescription") Mô tả ngắn
        input(type="text" id="shortDescription" name="shortDescription" value="Có đầy đủ các quyền") 
      .inner-group 
        label.inner-label(for="time") Phân quyền
        .inner-checkbox-list
          .inner-checkbox-item 
            +checkbox-30("","")
            label(for="") Xem trang Tổng quan
          .inner-checkbox-item 
            +checkbox-30("","")
            label(for="") Xem danh mục
          .inner-checkbox-item 
            +checkbox-30("","")
            label(for="") Tạo danh mục
          .inner-checkbox-item 
            +checkbox-30("","")
            label(for="") Sửa danh mục
          .inner-checkbox-item 
            +checkbox-30("","")
            label(for="") Xóa danh mục
      .inner-button.inner-two-col
        button Tạo mới
    .inner-back 
      a(href="#") Quay lại danh sách
  // End Section 8